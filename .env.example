# Server Configuration
PORT=3001
SERVE_CLIENT=true

# Path Configuration (for deployment under a subpath like /spades)
# Set to "/" for root deployment or "/spades" for subpath deployment
BASE_PATH=/

# CORS (only needed in development when client and server are separate)
CLIENT_URL=http://localhost:5173
